// Package services hosts domain-level service types (tickets, voice notes,
// documents, reviews). Handlers stay thin; services own the business logic
// and orchestrate database + AI worker calls.
package services
